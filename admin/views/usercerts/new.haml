%ul.nav.nav-tabs
  %li=link_to tag_icon(:list, pat(:list)), url(:usercerts, :index)
  %li.active=link_to tag_icon(:plus, pat(:new)), url(:usercerts, :new)

.tabs-content
  = form_for :usercert, url(:usercerts, :create), :class => 'form-horizontal' do |f|
    =partial 'usercerts/form', :locals => { :f => f }
