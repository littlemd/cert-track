%ul.nav.nav-tabs
  %li=link_to tag_icon(:list, pat(:list)), url(:certs, :index)
  %li.active=link_to tag_icon(:plus, pat(:new)), url(:certs, :new)

.tabs-content
  = form_for :cert, url(:certs, :create), :class => 'form-horizontal' do |f|
    =partial 'certs/form', :locals => { :f => f }
